import styled from "styled-components";

export const StyledQuestionImageAttributionText = styled.p`
  color: #222;
  margin-top: 0.5rem;
  font-size: 0.75rem;
  line-height: 1.3;
  display: block;
  position: relative;
  z-index: 100;
`;